pipeline {
    agent any
    stages {
        stage('Docker Build') {
            steps{
                script{
                sh "docker build -f Dockerfile -t node ."
            }
            }
        }
    }
}